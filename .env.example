GATEWAY_API_KEY=replace-with-a-random-secret
EFFICIENT_API_BASE=https://efficient.example.invalid/v1
EFFICIENT_API_KEY=replace-with-backend-key
CAPABLE_API_BASE=https://capable.example.invalid/v1
CAPABLE_API_KEY=replace-with-backend-key
# Optional host binding overrides; Compose publishes only loopback by default.
GATEWAY_IP=127.0.0.1
GATEWAY_PORT=4000
